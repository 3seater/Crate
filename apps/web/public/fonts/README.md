# Font Files

The app uses **Inter** (sans) loaded from Google Fonts via `next/font/google`.

No local font files are required. The Suisse Int'l woff2 files in this directory are no longer used and can be removed.
