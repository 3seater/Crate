/** Smoke test: e2e suite runs. Replace with Playwright/browser tests when added. */
import { describe, expect, it } from "vitest";

describe("e2e suite", () => {
  it("runs e2e config", () => {
    expect(true).toBe(true);
  });
});
